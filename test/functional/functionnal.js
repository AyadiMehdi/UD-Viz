const assert = require('assert');
//import {View3D} from "../../src/index";

describe('effects_split', function _() {
    it('should run', async () => {
        assert.ok(1+1 == 2);
        //const v = new View3D();
    });
});